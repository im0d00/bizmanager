# Nixpacks Configuration for BizManager
# This configuration disables the auto-detected build phase
# All installation and setup is handled by start.sh

[phases.setup]

[phases.install]
# Skip automatic npm install detection

[phases.build]
# Disable build phase - start.sh handles everything

[start]
cmd = "bash start.sh"
